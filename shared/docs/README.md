# Cross-platform architecture notes

Design notes and decisions that apply across platforms (URL-parsing edge cases, how the
tracking-parameter rule format is structured, share-intent handling conventions, etc.) go here
as they're written. Empty for now — the project is still Android-only.
